version: '3.8'

services:
  2048-game:
    build: .
    ports:
      - "8989:8989"
    volumes:
      - scores-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - SCORES_FILE=/app/data/scores.json

volumes:
  scores-data:
